# CLI

<Badge type="tip" text="Linux" /> <Badge type="tip" text="macOS" /> <Badge type="warning" text="In development" />

::: warning In development
A ready-to-download CLI upload script generated from the **Integrations** area is **not available
yet**. Until it ships, you can already upload from the command line with a single `curl` call —
see [Upload from the command line today](#upload-from-the-command-line-today) below.
:::

A lightweight upload script that hooks straight into your shell and file manager, so you can share
any file — or a screenshot — without leaving the terminal. Once released, you'll download it from
the **Integrations** area pre-filled with your instance URL and a personal upload token.
